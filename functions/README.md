# Cloudflare Pages Functions (fase 2+)

Esta carpeta está reservada para la API futura:

- `/api/checkout` — Stripe
- `/api/webhooks/stripe` — confirmación de pedidos
- `/api/download` — enlaces firmados a R2
- `/api/auth` — magic link para área de cliente

En fase 1 el sitio es 100% estático. No hace falta tocar nada aquí.