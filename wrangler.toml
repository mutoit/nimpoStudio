name = "nimpostudioweb"
compatibility_date = "2024-07-01"

[assets]
directory = "./dist"

# Custom domains (nimpo3dstudio.com / www) are configured in the Cloudflare dashboard
# under the Worker or via wrangler routes if needed.